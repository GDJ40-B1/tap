<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.btf.tap.mapper.SearchMapper">
	<!-- 숙소명 검색 조회 -->
	<select id="roomSearchList" parameterType="java.util.Map" resultType="com.btf.tap.vo.Room">
		SELECT
			room_id roomId,
			room_name roomName,
			room_intro roomIntro,
			min_day minDay,
			people_num peopleNum,
			room_price roomPrice
		FROM room
		<where>
			room_name LIKE 
			CONCAT ('%', #{keyword}, '%')
		</where>
		ORDER BY create_date DESC
		LIMIT #{beginRow}, #{rowPerPage}
	</select>
	
	<!-- 숙소 페이지 체크 -->
	<select id="totalRowCount" parameterType="String" resultType="int">
		SELECT
			COUNT(*)
		FROM 
		<choose>
			<when test="keyword == room">
				room
			</when>		
			<otherwise>
				attraction
			</otherwise>
		</choose>
		<where>
		
		</where>
	</select>	
	
	<!-- 명소 검색 조회 -->
	<select id="attractionSearchList" parameterType="String" resultType="com.btf.tap.vo.Attraction">
		SELECT
			attraction_id attractionId,
			attraction_category attractionCategory,
			attraction_name attractionName,
			attraction_content attractionContent
		FROM attraction
		<where>
			attraction_name LIKE
			CONCAT ('%', #{keyword}, '%')
		</where>
		ORDER BY create_date DESC
		LIMIT 0, 5
	</select>
	
	<!-- 해시 태그 테이블 조회 -->
	<select id="hashtagSearch" parameterType="String" resultType="com.btf.tap.vo.Hashtag">
		SELECT
			hashtag_target hashtagTarget,
			hashtag_name hashTagName,
			hashtag_target_category hashtagTargetCategory
		FROM hashtag
		<where>
			hashtag_name LIKE
			CONCAT ('%', #{keyword}, '%')
		</where>
		LIMIT 0, 5
	</select>
	
	<!-- 회원 검색어 기록 저장 -->
	<insert id="insertSearchKeyword" parameterType="com.btf.tap.vo.SearchHistory">
		INSERT INTO search_history(
			search_word,
			member_id,
			create_date
		) VALUES (
			#{searchWord},
			#{memberId},
			NOW()
		)
		
	</insert>
</mapper>